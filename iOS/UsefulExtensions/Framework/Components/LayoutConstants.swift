import Foundation
import UIKit

public struct LayoutConstants {
    //for ease of presentation
    public static let MIN_PADDING:CGFloat = 12; //minimum padding
    public static let MAX_PADDING:CGFloat = 48;//maximum padding
    public static let MIN_FONT_SIZE:CGFloat = 11;//minimum general font size
    public static let MIN_BODY_FONT_SIZE:CGFloat = 17;//minimum body font size
    public static let MIN_BUTTON_SIZE:CGFloat = 44;//minimum touch target size
}
